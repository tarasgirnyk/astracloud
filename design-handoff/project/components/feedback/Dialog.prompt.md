Centered modal with scrim — consultation request form, order confirmation.

```jsx
<Dialog open={open} onClose={() => setOpen(false)} title="Отримати консультацію">
  <Input label="Email" />
</Dialog>
```
