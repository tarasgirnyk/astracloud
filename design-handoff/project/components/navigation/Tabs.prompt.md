Segmented pill tab switcher — service type switch (VPS / Виділений сервер / Колокація) atop the pricing table.

```jsx
<Tabs tabs={[{value:'vps',label:'VPS'},{value:'dedicated',label:'Виділений сервер'}]} active={tab} onChange={setTab} />
```
